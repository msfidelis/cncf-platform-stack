- name: Put dashboard template
  template:
    src: dashboard.yml.j2
    dest: /tmp/dashboard.yml
    owner: root
    group: root
    mode: "0777"

- name: Apply dashboard
  shell: kubectl apply -f /tmp/dashboard.yml